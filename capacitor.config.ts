import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.trumpwalletloan.app",
  appName: "Trump Wallet Loan",
  webDir: "dist",
};

export default config;
